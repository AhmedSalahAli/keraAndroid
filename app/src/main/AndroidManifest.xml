<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.app.kera" >

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:name="com.app.kera.app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Fab_Bottom_app_bar.NoActionBar" >

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name="com.app.kera.attendanceHistory.AttendanceHistory" />
        <activity android:name="com.app.kera.qrCode.QRActivity" />

        <service
            android:name="com.app.kera.data.network.MyFirebaseMessagingService"
            android:exported="false" >
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name="com.app.kera.keraLuncher.KeraConfigrationsLuncher" />
        <activity android:name="com.app.kera.visitor.SideViewMain" />
        <activity
            android:name="com.app.kera.addLocation.AddLocation"
            android:theme="@style/Theme.Fab_Bottom_app_bar.fullscreen" />
        <activity android:name="com.app.kera.dailyReport.ui.DailyReportActivity" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name="com.app.kera.teacherDailyReport.writeReport.WriteReportActivity"
            android:label="@string/title_activity_write_report"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name="com.app.kera.events.ui.eventsdetails.EventsDetailsActivity" />
        <activity android:name="com.app.kera.events.ui.EventsActivity" />
        <activity android:name="com.app.kera.registration.screen4.Registration4Activity" />
        <activity android:name="com.app.kera.registration.screen3.Registration3Activity" />
        <activity
            android:name="com.app.kera.registration.screen1.Registration1Activity"
            android:theme="@style/Theme.Fab_Bottom_app_bar.NoActionBar" />
        <activity
            android:name="com.app.kera.pincode.PinCodeActivity"
            android:label="@string/title_activity_pin_code"
            android:theme="@style/Theme.Fab_Bottom_app_bar.NoActionBar" />
        <activity android:name="com.app.kera.main.ui.MainActivity" />
        <activity
            android:name="com.app.kera.login.ui.LoginActivity"
            android:windowSoftInputMode="adjustResize" /> <!-- <activity android:name=".main.ui.MainActivity" /> -->
        <activity android:name="com.app.kera.registration.screen2.Registration2Activity" />
        <activity android:name="com.app.kera.meals.MealsActivity" />
        <activity android:name="com.app.kera.medical.MedicalReportActivity" />
        <activity android:name="com.app.kera.education.EducationActivity" />
        <activity android:name="com.app.kera.schoolDetails.ui.SchoolDetailsActivity" />
        <activity
            android:name="com.app.kera.meals.details.MealsDetailsActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name="com.app.kera.splash.ui.SplashActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.app.kera.teacherMedicalReport.TeacherMedicalReportActivity" />
        <activity android:name="com.app.kera.teacherDailyReport.ui.TeacherDailyReportActivity" />
        <activity android:name="com.app.kera.contactUs.ContactUsActivity" />
        <activity android:name="com.app.kera.teacherMedicalReport.writeMedicalReport.WriteMedicalReportActivity" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>